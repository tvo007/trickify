// import {useMediaQuery} from '@mui/material';
// //   const isMdUp = useMediaQuery (theme.breakpoints.up ('md'));

// export const handleBreakpointUp = (theme, breakpoint) => {
//   const mediaQuery = useMediaQuery (theme.breakpoints.up (breakpoint));
//   return mediaQuery;
// };

// export const handleBreakpointDown = (theme, breakpoint) => {
//   const mediaQuery = useMediaQuery (theme.breakpoints.down (breakpoint));
//   return mediaQuery;
// };

// export const handleBreakpointBetween = (theme, lower, upper) => {
//   const mediaQuery = useMediaQuery (theme.breakpoints.between (lower, upper));
//   return mediaQuery
// };

const breakpoints = {
  extraSmall: 'xs',
  small: 'sm',
  medium: 'md',
  large: 'lg',
  extraLarge: 'xl',
};

export default breakpoints;
